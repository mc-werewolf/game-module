# Werewolf-GameEngine

## Development

- Package manager: `pnpm@10.31.0`
- Build pipeline: TypeScript declarations (`tsc`) + JavaScript bundling (`esbuild` via `build.mjs`)

### Commands

```bash
pnpm install
pnpm run build
```
